# HERO-M 恢复正常控制

用户明确要求恢复正常 HERO-M 控制，保留 HERO-C 的控制参数；确认遥控器已关闭，24V 保持接通。用户说明上车后改用无线调试，要求不再重复做烧录前备份。

- 当前源码构建检查通过，正式镜像 532812 字节，SHA-256 `e067786551bdcb46b2a5dedad76c7a6e53720bd8d757755a92107524654ba345`。准备、只接收和仅音乐模式均未启用。
- pitch 配置为 CAN2、3510、编号 5。发送路由将反馈 ID 0x205～0x208 分配给控制帧 0x1FF，0x205 使用第一个大端有符号 16 位字段；CAN2 摩擦轮 1～4 保持 0x200 分组。
- OpenOCD 500 kHz 在 24V 未断开的条件下完成烧录。实际擦写扇区 0～4，止于 0x0809FFFF，未擦写 0x080C0000 起的校准区。
- 532812 字节程序完整读回与候选一致；整个 262144 字节校准区与烧录前一致。没有更改 PID、零位、方向或限位参数。
- 启动后首次调试读取出现重复失真数据，OpenOCD 曾报告 double fault/lockup。随后 DAP 自身 CMD_INFO 失败。用户重插电脑端 USB 后，100 kHz、固定 32 位读取恢复正常；读到 CFSR/HFSR 等故障寄存器均为 0，启动日志正常，不能把首次调试输出认定为已证实的固件崩溃。
- 检查期间曾暂停主控，恢复时约第 8.392 秒记录了三条 CAN FIFO0 消息丢失日志；这保留为调试干扰窗口，不能描述为全程完全无丢帧。
- 此后关闭调试自动轮询、不暂停主控，连续两次读取均显示主控运行、故障锁 0、故障寄存器 0。CAN1 接收 70016→548689、发送请求 11578→90932；CAN2 接收 58194→455544、发送请求 11610→91074。两次驱动发送失败及软件接收队列丢帧计数均 0。
- 遥控保持关闭，输入帧 0。两次 CAN2 的 0x200/0x1FF 指令缓冲均为零；IMU 校准 loaded=1、saved=0、sequence=3、lastError=0，温度 41.625→40.125℃。
- 2026-09-06 22:21:50（北京时间）通知用户开始遥控测试；22:22:35 用户回复“pitch正常，现在蜂鸣器在叫”。2026-09-07 用户补充澄清：当时已确认整车运动正常，包括底盘和云台俯仰，不仅是单轴响应。编号 5 的非零 0x1FF 指令响应已由现场运行确认；本条属于用户实车反馈，不是调试计数推断。

## HERO 实车记录与接线版本

| 记录 | 适用硬件与结论 |
| --- | --- |
| 历史提交 `5502578`：引入电机实例装配层（HERO 上车通过） | 当时“上车运行正常”针对旧 HERO-C、C 板接线。用户于 2026-09-07 明确补充该适用范围。 |
| 2026-09-06 22:22 本次 Zephyr 实车运行 | 当前 HERO 使用 M 板与第二版副板；用户确认整车运动正常，包括俯仰。整车已彻底改为 M 板接线，旧 C 板接线和对应配置不能直接用于现在这台车。 |

HERO-C 代码保留为历史硬件目标；保留部分 HERO-C 控制参数不表示两种接线兼容。旧提交保持原样，本表补充其硬件适用范围。

随后已烧入关闭 HERO 低压报警、调整音乐按键的 533136 字节版本，读回及启动检查通过，详见 [报警与按键记录](AlarmButtons-20260906.md)。该记录中的 SD 未就绪是当晚检查结果；用户次日确认已找回卡，但尚未补充新版实体按钮播放、切歌及停止的验收结果。

整车运动正常不扩展为长时间高负载、所有外设或故障停机情景均已验证；这些仍需各自的实测记录。

## 2026-09-07 提交前检查

- SD 协议及诊断格式、陀螺零偏、HERO-M 与旧目标坐标转换、12 组控制安全回归、低压报警与音乐按钮逻辑检查均通过。
- 当前 HERO-M 与 HERO-C Zephyr 构建目录检查通过，Ninja 均返回无待构建项；本次未重新执行七目标全新构建。
- 当前 HERO-M 固件 SHA-256 与上一晚报警按键版的烧录验证一致：`c7e4d8ea0b3b081650f288a628c09fcac63bc839a3a95cd30cce4b648f2e064d`。
- 本次整理未操作硬件，未修改主分支或移除 Keil 工作流。

证据目录：`local/cache/zephyr-preflight-hardware/20260906/normal-output/`，包含固件、Flash 读回、`verify.json`、`runtime-verification.json`、运行读取脚本与日志。
